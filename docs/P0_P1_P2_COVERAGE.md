# P0 / P1 / P2 Coverage

## P0
Validated observations/embeddings/predictions; pluggable encoder/classifier/anomaly/calibration contracts; prototype classification; open-set/OOD; anomaly scoring; calibration and uncertainty; similarity; clustering; temporal aggregation; batch/stream inference; bounded verified artifacts; CPU backend; benchmarks; optional HTTP serving.

## P1
Masked-context self-supervision; continual learning; domain adaptation; metric learning; novelty detection; learned segmentation; sequence classification; ensembles; explainability; drift monitoring; dataset manifests; group-aware split.

## P2
Foundation-model contract and pooling; temporal self-attention and transformer-style encoding; variance autoencoder; contrastive projector; active learning; embedding-prototype zero-shot classification; soft-label distillation; symmetric quantization/fake quantization; quantization-aware reference prototype training; magnitude pruning; edge parameter/latency profiling; deterministic synthetic adapter; wasm32 core gate.

All capabilities are implemented as standalone reference functionality over caller-supplied numeric features.

## Cross-cutting reliability (0.3)

Typed bounded micro-ontology; reference-schema relation/cardinality validation; semantic mapping from inference results; deterministic recurring-sequence/co-occurrence detection; SHA-256 canonical input/context/result certificates; fixed-point decision digests; opt-in verified pipeline; exact/decision-equivalent/non-reproducible replay classification.

